{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "db:push": "supabase db push",
    "db:reset": "supabase db reset",
    "seed:maps": "tsx seeds/load-maps.ts",
    "deploy:fns": "supabase functions deploy verify-receipt && supabase functions deploy tournament-scheduler"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
